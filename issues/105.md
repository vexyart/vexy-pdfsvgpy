We now use 'pdf_m' rather than 'pdf-m' for qualified format names (= we use underscores instead of hyphens). Therefore, create CLI commands like `pdf_m` which would be a shortcut for `convert --voutput pdf_m`, inheriting all other options. Implement this for all possible format names. 