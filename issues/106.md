## Text treatment

We should be able to: 

--text_as "text": preserve text at any case, even if fonts cannot be embedded (this is "semantic preservation")
--text_as "paths" : always convert text to paths (this is "visual preservation")
--text_as "fonts" : preserve embedded fonts if possible, if not possible, convert text to paths (default, this is "balanced preservation")